# EXERCÍCIO 1

# A distância (em km) e o tempo (em min) são, respectivamente: 
d = 10 
t = 43.5

# Como 1 milha = 1.61 km e 1 h = 60 min, a velocidade média em milhas/h é:

vmed = (d/1.61)/(t/60)
print('a velocidade média é de ', vmed, ' milhas/h')

# Sendo assim, o tempo médio por milha (em minutos) é de:

tmed = (vmed**(-1))*60
print('o tempo médio é de ', tmed, ' min/milha')

# EXERCÍCIO 2

# Sabe-se que as velocidades da luz e do som (em m/s) são, respectivamente, de:
v_luz = 3*10**8
v_som = 343

# Como ambos percorrem a mesma distância d, com o som levando 3 segundos a mais (t_som = t_luz + 3), então:
# d = 3*(10**8)*t_luz = 343*(t_luz+3)
t_luz = 1029/299999657

# Logo a distãncia d em que os fogos foram lançados é de:
d = 3*(10**8)*t_luz
print('a distância d em que os fogos foram lançados é de ', d, ' metros')

# EXERCÍCIO 3

import math

# Função é y = 3*x**2 - 4*x - 10, logo:
a = 3
b = -4
c = -10

# Primeiramente devemos calcular delta:
delta = (b**2)-4*a*c

# Para então obter os zeros da função:
x1 = (-b+math.sqrt(delta))/(2*a)
x2 = (-b-math.sqrt(delta))/(2*a)
print('as raízes são ',x1,' e ',x2)

# EXERCÍCIO 4

import math

# A altura do poste e a sombra que ela projeta ao meio-dia (em metros) são, respectivamente, de:
h = 5
s = 0.5

# Logo o ângulo zenital teta_z do Sol neste horário é dado por tan(teta_z) = s/h.
# Como o Python originalmente fornece o ângulo em radianos, há de se fazer a conversão para graus.
# Para tal, basta multiplicar o resultado por (180/pi):
teta_z = math.atan(s/h)*(180/math.pi)
print('o ângulo zenital do Sol ao meio-dia é de :', teta_z, ' graus')

# EXERCÍCIO 5

# Meus valores de massa (em kg) e altura (em metros) são respectivamente de:
M = 69
A = 1.79

# Então meu IMC é:
IMC = M/(A**2)
print('meu IMC é de ', IMC)

# Já no caso do bebê:
M = 11
A = 0.7
IMC = M/(A**2)
print('o IMC do bebê é de ', IMC)

# EXERCÍCIO 6

# A partir da equação de Torricelli, pode-se obter a velocidade final do objeto usando-se apenas a altura de queda.
# Assumindo que o objeto inicialmente estava em repouso e sabendo que ele caiu de 3 metros:

h = 3 # altura em metros
vo = 0 # velocidade inicial (repouso)
g = 9.8 # aceleração gravitacional em m/(s**2)
v = (vo**2+2*g*h)**0.5
print('a velocidade final do objeto é de: ', v, ' m/s')

# O tempo que o objeto leva para cair, por sua vez, pode ser obtido a partir de:
t = (v-vo)/g
print('o tempo que o objeto demora para cair é de: ', t, ' segundos')
